name: release

on:
  workflow_dispatch:

jobs:
  tag:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: checkout
        uses: actions/checkout@v2

      - name: set output
        id: vars
        run: echo "::set-output name=date::$(date +'+%Y.%-m.%-d')"

      - name: create tag
        uses: actions/github-script@v5
        with:
          script: |
            github.rest.git.createRef({
              owner: context.repo.owner,
              repo: context.repo.repo,
              ref: 'refs/tags/${{ steps.vars.outputs.date }}',
              sha: context.sha
            })

# TODO
# on schedule, download entries. if entries are updated, commit/push/create release
# also on workflow dispatch
# https://stackoverflow.com/a/61018964
# https://github.com/marketplace/actions/publish-firefox-add-on
# https://github.com/browser-actions/release-chrome-extension
